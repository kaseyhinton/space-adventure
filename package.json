{
  "scripts": {
    "start:build": "cd dist && es-dev-server --open",
    "build": "rimraf dist && rollup -c rollup.config.js",
    "start": "es-dev-server --app-index index.html --node-resolve --open --watch"
  },
  "devDependencies": {
    "@open-wc/building-rollup": "^0.15.1",
    "es-dev-server": "^1.5.0",
    "rimraf": "^2.6.3",
    "rollup": "^1.15.4"
  },
  "name": "space-adventure",
  "version": "0.0.0",
  "description": "Webcomponent space-adventure following open-wc recommendations",
  "author": "space-adventure",
  "license": "MIT",
  "dependencies": {
    "@material/mwc-ripple": "^0.12.0",
    "lit-element": "^2.0.1",
    "lit-html": "^1.0.0",
    "wc-typed-js": "^1.0.1"
  }
}
